import { JarabeFilterPipe } from './jarabe-filter.pipe';

describe('JarabeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JarabeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
