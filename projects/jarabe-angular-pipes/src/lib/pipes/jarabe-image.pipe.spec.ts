import { JarabeImagePipe } from './jarabe-image.pipe';

describe('JarabeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new JarabeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
